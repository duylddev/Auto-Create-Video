<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=1080, height=1920">
  <title>{{TITLE}}</title>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div id="stage"
       data-composition-id="news-video"
       data-width="1080"
       data-height="1920"
       data-start="0"
       data-duration="{{TOTAL_DURATION}}">

    <audio id="voice"
           class="clip"
           data-start="0"
           data-duration="{{TOTAL_DURATION}}"
           data-track-index="0"
           src="voice.mp3"></audio>

    {{SCENES}}
  </div>
  <script>
    // Create + register timeline synchronously so HyperFrames runtime can find it
    window.__timelines = window.__timelines || {};
    window.__timelines["news-video"] = gsap.timeline({ paused: true });
  </script>
  <script src="animations.js"></script>
</body>
</html>
